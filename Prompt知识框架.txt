要求:
1. 使用统一的树形结构展示层级关系:
   - 使用 "├── " 表示非最后项
   - 使用 "└── " 表示最后一项 
   - 子级缩进使用 "│   " 保持对齐
   - 最多支持4层缩进

2. 节点命名规范:
   - 每个节点名称使用简短的名词或名词短语
   - 节点名称不超过20个字符
   - 不在树形结构中包含描述性文本

3. 层级规范:
   - 第一层：主题(必选，1个)
   - 第二层：主要分类(必选，2-5个)
   - 第三层：子分类(可选，每个主要分类下不超过5个)
   - 第四层：具体项目(可选，每个子分类下不超过5个)

输出格式:
第一部分：知识框架图
[使用规范的树形结构展示层级关系]

第二部分：核心概念定义
格式要求:
- 使用 "概念:定义" 的格式
- 每个定义限制在15-20个字以内
- 只解释概念本身,不包含上下文信息或例子
- 按照框架图中的层级顺序列出
- 使用陈述句,避免比喻或修辞

示例:
第一部分：知识框架图
Transformer
├── 编码器
│   ├── 自注意力层
│   └── 前馈神经网络
└── 解码器
    ├── 掩码自注意力
    └── 交叉注意力

第二部分：核心概念定义
Transformer: 基于自注意力机制的神经网络架构
编码器: 将输入序列转换为连续表示的组件
解码器: 根据编码器输出生成目标序列的组件
自注意力层: 计算序列内部元素相互关系的机制
前馈神经网络: 对每个位置独立进行非线性变换的网络层
掩码自注意力: 防止解码时访问未来信息的注意力机制
交叉注意力: 解码器处理编码器输出信息的注意力层

请帮我将以下内容整理成分层的知识框架：